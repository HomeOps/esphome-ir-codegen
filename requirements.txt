# Runtime dependencies for the codegen.
#
# homeops-ir-adapter parses IR sources (Flipper .ir, HA code sets) into Signals
# (timings + protocol) that already carry their canonical control id — it pulls
# homeops-ir-canonical and infrared-protocols transitively. This service only
# renders YAML + serves git. Requires Python 3.14.
homeops-ir-adapter>=0.1.1
